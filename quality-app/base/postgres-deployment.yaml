apiVersion: apps/v1
kind: Deployment
metadata:
  name: quality-postgres
  namespace: wms-proto
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quality-postgres
  template:
    metadata:
      labels:
        app: quality-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:12.8
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: quality
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
