apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: audit-app
  namespace: wms-proto
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /api/audit
            pathType: Prefix
            backend:
              service:
                name: audit-app
                port:
                  number: 8085
