apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-postgres
  namespace: wms-proto
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-postgres
  template:
    metadata:
      labels:
        app: order-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:12.6
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: om
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
