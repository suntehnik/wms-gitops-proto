apiVersion: v1
kind: Service
metadata:
  name: order-hazelcast
  namespace: wms-proto
spec:
  selector:
    app: order-hazelcast
  ports:
    - name: hazelcast
      port: 5701
      targetPort: 5701
