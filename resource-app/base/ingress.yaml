apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: resource-app
  namespace: wms-proto
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /api/resource
            pathType: Prefix
            backend:
              service:
                name: resource-app
                port:
                  number: 8140
