apiVersion: v1
kind: Service
metadata:
  name: resource-zookeeper
  namespace: wms-proto
spec:
  selector:
    app: resource-zookeeper
  ports:
    - name: zookeeper
      port: 2181
      targetPort: 2181
