apiVersion: v1
kind: Service
metadata:
  name: resource-hazelcast
  namespace: wms-proto
spec:
  selector:
    app: resource-hazelcast
  ports:
    - name: hazelcast
      port: 5701
      targetPort: 5701
