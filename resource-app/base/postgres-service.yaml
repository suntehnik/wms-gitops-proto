apiVersion: v1
kind: Service
metadata:
  name: resource-postgres
  namespace: wms-proto
spec:
  selector:
    app: resource-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
