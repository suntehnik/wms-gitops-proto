apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: inbound-app
  namespace: wms-proto
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /api/inbound
            pathType: Prefix
            backend:
              service:
                name: inbound-app
                port:
                  number: 8130
