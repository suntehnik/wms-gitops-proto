apiVersion: v1
kind: Service
metadata:
  name: inbound-postgres
  namespace: wms-proto
spec:
  selector:
    app: inbound-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
