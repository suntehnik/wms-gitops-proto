apiVersion: apps/v1
kind: Deployment
metadata:
  name: inbound-postgres
  namespace: wms-proto
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inbound-postgres
  template:
    metadata:
      labels:
        app: inbound-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:12.8
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: inbound
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
