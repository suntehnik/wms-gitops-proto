apiVersion: v1
kind: Service
metadata:
  name: inventory-zookeeper
  namespace: wms-proto
spec:
  selector:
    app: inventory-zookeeper
  ports:
    - name: zookeeper
      port: 2181
      targetPort: 2181
