apiVersion: v1
kind: Service
metadata:
  name: inventory-postgres
  namespace: wms-proto
spec:
  selector:
    app: inventory-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
