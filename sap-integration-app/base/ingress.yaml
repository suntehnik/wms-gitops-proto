apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sap-integration-app
  namespace: wms-proto
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /api/sap-integration
            pathType: Prefix
            backend:
              service:
                name: sap-integration-app
                port:
                  number: 8080
