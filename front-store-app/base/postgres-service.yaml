apiVersion: v1
kind: Service
metadata:
  name: front-store-postgres
  namespace: wms-proto
spec:
  selector:
    app: front-store-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
