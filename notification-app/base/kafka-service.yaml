apiVersion: v1
kind: Service
metadata:
  name: notification-kafka
  namespace: wms-proto
spec:
  selector:
    app: notification-kafka
  ports:
    - name: kafka
      port: 9094
      targetPort: 9094
