apiVersion: apps/v1
kind: Deployment
metadata:
  name: pim-kafka
  namespace: wms-proto
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pim-kafka
  template:
    metadata:
      labels:
        app: pim-kafka
    spec:
      containers:
        - name: kafka
          image: confluentinc/cp-kafka:5.4.4
          ports:
            - containerPort: 9094
          env:
            - name: KAFKA_BROKER_ID
              value: "1"
            - name: KAFKA_ZOOKEEPER_CONNECT
              value: pim-zookeeper:2181
            - name: KAFKA_ADVERTISED_LISTENERS
              value: PLAINTEXT://pim-kafka:9094
            - name: KAFKA_LISTENER_SECURITY_PROTOCOL_MAP
              value: PLAINTEXT:PLAINTEXT
            - name: KAFKA_INTER_BROKER_LISTENER_NAME
              value: PLAINTEXT
            - name: KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR
              value: "1"
            - name: KAFKA_LISTENERS
              value: PLAINTEXT://0.0.0.0:9094
