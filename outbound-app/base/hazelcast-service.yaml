apiVersion: v1
kind: Service
metadata:
  name: outbound-hazelcast
  namespace: wms-proto
spec:
  selector:
    app: outbound-hazelcast
  ports:
    - name: hazelcast
      port: 5701
      targetPort: 5701
