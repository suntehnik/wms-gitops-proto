apiVersion: apps/v1
kind: Deployment
metadata:
  name: outbound-postgres
  namespace: wms-proto
spec:
  replicas: 1
  selector:
    matchLabels:
      app: outbound-postgres
  template:
    metadata:
      labels:
        app: outbound-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:12.6
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: wms
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
