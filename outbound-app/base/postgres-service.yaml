apiVersion: v1
kind: Service
metadata:
  name: outbound-postgres
  namespace: wms-proto
spec:
  selector:
    app: outbound-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
