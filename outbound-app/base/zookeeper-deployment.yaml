apiVersion: apps/v1
kind: Deployment
metadata:
  name: outbound-zookeeper
  namespace: wms-proto
spec:
  replicas: 1
  selector:
    matchLabels:
      app: outbound-zookeeper
  template:
    metadata:
      labels:
        app: outbound-zookeeper
    spec:
      containers:
        - name: zookeeper
          image: confluentinc/cp-zookeeper:5.4.4
          ports:
            - containerPort: 2181
          env:
            - name: ZOOKEEPER_CLIENT_PORT
              value: "2181"
            - name: ZOOKEEPER_TICK_TIME
              value: "2000"
