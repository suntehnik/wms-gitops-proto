apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: outbound-app
  namespace: wms-proto
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /api/outbound
            pathType: Prefix
            backend:
              service:
                name: outbound-app
                port:
                  number: 8080
