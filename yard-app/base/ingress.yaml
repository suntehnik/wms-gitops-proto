apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: yard-app
  namespace: wms-proto
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /api/yard
            pathType: Prefix
            backend:
              service:
                name: yard-app
                port:
                  number: 8155
